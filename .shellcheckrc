# Providers and the orchestrator source siblings via a dynamic path
# ($(dirname "${BASH_SOURCE[0]}")/...), which ShellCheck cannot resolve
# statically. SC1091 ("not following sourced file") is pure noise here, so we
# disable it. All other checks remain enabled and gating.
disable=SC1091
